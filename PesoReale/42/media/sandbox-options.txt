VERSION = 1,

-- ================================================================
-- Peso Reale - Sandbox Options  (B42 format)
-- Mode = 1 -> Multiplier  (e.g. 0.5 = half the original weight)
-- Mode = 2 -> Fixed value (e.g. 0.05 = every item weighs exactly 0.05)
-- ================================================================

-- === ARMI / WEAPONS ===
option PesoReale.WeaponMode
{
    type = integer,
    min = 1,
    max = 2,
    default = 1,
    page = PesoReale,
    translation = PesoReale_WeaponMode,
}

option PesoReale.WeaponValue
{
    type = double,
    default = 1.0,
    min = 0.01,
    max = 20.0,
    page = PesoReale,
    translation = PesoReale_WeaponValue,
}

-- === MUNIZIONI / AMMO ===
option PesoReale.AmmoMode
{
    type = integer,
    min = 1,
    max = 2,
    default = 1,
    page = PesoReale,
    translation = PesoReale_AmmoMode,
}

option PesoReale.AmmoValue
{
    type = double,
    default = 1.0,
    min = 0.001,
    max = 5.0,
    page = PesoReale,
    translation = PesoReale_AmmoValue,
}

-- === VESTIARIO / CLOTHING ===
option PesoReale.ClothingMode
{
    type = integer,
    min = 1,
    max = 2,
    default = 1,
    page = PesoReale,
    translation = PesoReale_ClothingMode,
}

option PesoReale.ClothingValue
{
    type = double,
    default = 1.0,
    min = 0.01,
    max = 10.0,
    page = PesoReale,
    translation = PesoReale_ClothingValue,
}

-- === CIBO / FOOD ===
option PesoReale.FoodMode
{
    type = integer,
    min = 1,
    max = 2,
    default = 1,
    page = PesoReale,
    translation = PesoReale_FoodMode,
}

option PesoReale.FoodValue
{
    type = double,
    default = 1.0,
    min = 0.01,
    max = 10.0,
    page = PesoReale,
    translation = PesoReale_FoodValue,
}

-- === MEDICINALI / MEDICAL ===
option PesoReale.MedicalMode
{
    type = integer,
    min = 1,
    max = 2,
    default = 1,
    page = PesoReale,
    translation = PesoReale_MedicalMode,
}

option PesoReale.MedicalValue
{
    type = double,
    default = 1.0,
    min = 0.001,
    max = 5.0,
    page = PesoReale,
    translation = PesoReale_MedicalValue,
}

-- === ALTRO / MISC ===
option PesoReale.MiscMode
{
    type = integer,
    min = 1,
    max = 2,
    default = 1,
    page = PesoReale,
    translation = PesoReale_MiscMode,
}

option PesoReale.MiscValue
{
    type = double,
    default = 1.0,
    min = 0.001,
    max = 10.0,
    page = PesoReale,
    translation = PesoReale_MiscValue,
}
